// services/config.ts

/**
 * URL base para todas as chamadas de API.
 * Deve ser ajustada para o ambiente de produção quando o backend for implantado.
 */
export const API_BASE_URL = 'http://localhost:3000/api';